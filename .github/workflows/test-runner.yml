name: SENETECH Runner Check
on:
  push:
    branches: [main]
    paths: ['.github/workflows/test-runner.yml']
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo SENETECH-RUNNER-OK
